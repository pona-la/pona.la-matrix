issuer: http://mi.pona.la/dex

storage:
  type: sqlite3
  config:
    file: /var/dex/dex.db

# HTTP service configuration
web:
  http: 0.0.0.0:5556
  issuer: mi pona
  logoURL: /register/static/logo.png
  dir: ""
  theme: light

logger:
  level: "info"
  format: "json"
  # These are excluded for GDPR compliance.
  excludeFields: [email, username, preferred_username, groups]

staticClients:
  - name: 'ilo Kito'
    id: ilo-kito
    secretEnv: ILO_KITO
    redirectURIs:
      - 'https://kito.pona.la/user/oauth2/mi.pona.la/callback'


connectors:
  - type: ldap
    id: ldap
    name: LDAP
    config:
      host: lldap:3890
      insecureNoSSL: true
      insecureSkipVerify: true
      bindDN: uid=admin,ou=people,dc=pona,dc=la
      bindPW: $LDAP_ADMIN_PASS
      userSearch:
        baseDN: ou=people,dc=pona,dc=la
        username: uid
        idAttr: uid
        emailAttr: mail
        nameAttr: displayName
        preferredUsernameAttr: uid
      groupSearch:
        baseDN: ou=groups,dc=pona,dc=la
        filter: "(objectClass=groupOfUniqueNames)"
        userMatchers:
          - userAttr: DN
            groupAttr: member
        nameAttr: cn
